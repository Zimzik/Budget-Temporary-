<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:padding="16dp">

    <TextView
        android:id="@+id/tv_rev_date"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:hint="Date"
        android:layout_marginLeft="4dp"
        android:textSize="20sp"
        app:layout_constraintLeft_toLeftOf="parent"/>
    <TextView
        android:layout_width="100dp"
        android:layout_height="1dp"
        app:layout_constraintTop_toBottomOf="@id/tv_rev_date"
        app:layout_constraintLeft_toLeftOf="@id/tv_rev_date"
        android:background="#aaa"/>

    <EditText
        android:id="@+id/et_rev_descr"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:hint="Description"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_rev_date"/>

    <EditText
        android:id="@+id/et_rev_money"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:hint="Money"
        android:inputType="number"
        app:layout_constraintTop_toBottomOf="@id/et_rev_descr"/>
</android.support.constraint.ConstraintLayout>